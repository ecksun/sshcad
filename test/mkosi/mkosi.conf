[Distribution]
Distribution=debian
Release=bookworm

[Output]
Format=disk
ImageId=sshca-serv-test
OutputDirectory=build

[Content]
ExtraTrees=
	trees/test
	trees/build

Packages=
    systemd
    systemd-container
    swtpm
    swtpm-tools
    openssh-client
    sqlite3
    curl
